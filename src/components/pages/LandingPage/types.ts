export type SupportItemType = {
  icon: string;
  iconColor: string;
  title: string;
  text: string;
  button: string;
  buttonType: string;
  href: string;
};

export interface StepProps {
  date: string;
  title: string;
}

export type TradingStepType = {
  accent: string;
  text: string;
};
