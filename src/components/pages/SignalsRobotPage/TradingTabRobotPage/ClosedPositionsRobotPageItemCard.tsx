import React from 'react';

import { SectionType } from '../types';

interface Props {
  item: any;
  robot: any;
  activeTab: SectionType;
}

export const ClosedPositionsRobotPageItemCard: React.FC<Props> = () => {
  return (null);
};